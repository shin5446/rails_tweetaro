<h3>以下の内容で、Tweetする。</h3>

<%= form_with(model: @tweet, url: tweets_path, local: true) do |form| %>

  <%= form.hidden_field :content %>

  
  <p>内容:<%= @tweet.content %></p>
  <%= form.submit  "Tweetする" %>
<% end %>